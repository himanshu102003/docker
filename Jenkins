pipeline {
    agent { label 'java_agent' }  // Ensure this matches your agent's label
 
    stages {
        stage('Hello World') {
            steps {
                echo 'Hello, World from Docker Agent!'
            }
        }
    }
}
